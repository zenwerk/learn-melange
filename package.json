{
  "name": "melange-calculator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:all": "dune runtest && tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^6.0.0",
    "vitest": "^4.1.4"
  }
}
