{
  "name": "melange-calculator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:all": "dune runtest && vitest run"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^4.1.4"
  }
}
